import React from 'react';
import AppNavigator from './navigation';

const App = (): JSX.Element => {
  return (
    <React.StrictMode>
      <AppNavigator />
    </React.StrictMode>
  );
};

export default App;
